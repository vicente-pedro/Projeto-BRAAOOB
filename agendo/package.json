{
  "name": "agendo",
  "version": "1.0.0",
  "private": true,
  "description": "Gerenciador de Tarefas — Equipe 05",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run build && cross-env NODE_ENV=production npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3"
  }
}
